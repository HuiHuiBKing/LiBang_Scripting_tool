1.使用pyinstaller打包
	pip install pyinstaller
default cmd执行 pyinstaller -F x.py # 单个文件打包
                           -D 	   # 多个文件打包
                           --noconsole  # 运行程序时不弹出cmd窗口
                           -icon x.jpg  # 指定程序图标
		
